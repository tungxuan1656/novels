# feat-020 — Fastlane iOS Diawi Distribution

## Goal

`bundle exec fastlane ios distribute` builds a signed IPA from `apps/novels.xcodeproj` (scheme `novels`) and uploads it to Diawi with no hardcoded secrets.

## Scope

- `Gemfile` (new): `rubygems` + `gem "fastlane"` (+ `Gemfile.lock` generated by bundler, committed for reproducible installs).
- `fastlane/Appfile` (new): `app_identifier "com.tungxuan.novels"`, team `M5U4E4H84J`.
- `fastlane/Fastfile` (new, iOS only): adapted from the RN file — `project` (not workspace), scheme `novels`, lanes `prepare`/`build`/`upload`/`distribute`, Diawi upload + poll via `curl`, token from `ENV["DIAWI_TOKEN"]`, timeouts 180.
- `.gitignore` (append): fastlane artifacts (`fastlane/report.xml`, `fastlane/Preview.html`, `fastlane/screenshots`, `fastlane/test_output`).

## Non-goals

- No Android lanes (repo is iPhone-only per `docs/decisions/ios-scope.md`).
- No hardcoded Diawi token (per `SECURITY.md` — token via `ENV["DIAWI_TOKEN"]` only).
- No CI workflow, no TestFlight/App Store Connect, no match/cert management beyond automatic signing.
- No Xcode project changes.

## Acceptance

- [x] `ruby -c fastlane/Fastfile` PASS.
- [x] `bundle exec fastlane lanes` lists `ios prepare`/`build`/`upload`/`distribute`.
- [x] No hardcoded token in repo (`grep DIAWI_TOKEN` only `ENV[...]`, old `5As7ju0u` 0 hits).
- [x] No android references in `fastlane/` (grep case-insensitive 0 hits).
- [x] `./init.sh` full PASS.

## Relevant docs

- `ARCHITECTURE.md` §1/§5 (scheme `novels`, iOS 26.5, `init.sh` canonical).
- `SECURITY.md` (no secrets in repo).
- `docs/decisions/ios-scope.md` (iPhone-only, no Android).

## Plan (inline)

Inline (bounded: 3 new files + 1 `.gitignore` append, 1 workspace, <200 lines; only the `>=4 files` signal fires, so no separate `docs/plans/` file). Direct execution, no specialist — known interfaces, no discovery left. File ownership: orchestrator owns all (no parallel writers).

1. Orchestrator: branch `feat/020-fastlane-ios-diawi` + feat-020 `active` + baseline `./init.sh --quick`.
2. Orchestrator: write `Gemfile` + `fastlane/Appfile` + `fastlane/Fastfile`, append `.gitignore`.
3. Orchestrator: `ruby -c` + `bundle exec fastlane lanes` + grep guards + `./init.sh` full + commit + close.

## Verify

- `ruby -c fastlane/Fastfile`
- `bundle exec fastlane lanes`
- `./init.sh` (full)

## Handoff

- State: done — direct execution (no specialist), `ruby -c` Syntax OK + `fastlane lanes` 4/4 + guards (0 hardcoded token, 0 android refs) + `./init.sh` full PASS.
- Evidence: `Gemfile` + `Gemfile.lock`, `fastlane/Appfile` (`com.tungxuan.novels`/`M5U4E4H84J`), `fastlane/Fastfile` (iOS only, `project` not workspace, `ENV["DIAWI_TOKEN"]`), `.gitignore` fastlane artifacts; `feature_index.json` feat-020 done (zero active).
- Blockers: none.
- Next: repo idle — export `DIAWI_TOKEN` then `bundle exec fastlane ios distribute` (`output_name`/`export_method` overridable, default `app-release.ipa`/`development`).

<!-- harness-slim 1.4.0 · generated 2026-08-24 -->
